import * as React from "react";
import styled from "styled-components";
import { Layout } from "../components/Layout";

const PageStyles = styled.section``;

const RudysStory = () => {
  return (
    <Layout>
      <PageStyles></PageStyles>
    </Layout>
  );
};

export default RudysStory;
